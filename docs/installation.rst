.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install secken-sdk

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv secken-sdk
    $ pip install secken-sdk
